// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package me.rahimklaber.stellar.base.xdr


/**
 * SorobanAddressCredentialsWithDelegates's original definition in the XDR file is:
 * ```
 * struct SorobanAddressCredentialsWithDelegates
{
 SorobanAddressCredentials addressCredentials;
 SorobanDelegateSignature delegates<>;
};
 * ```
 */
data class SorobanAddressCredentialsWithDelegates(
    val addressCredentials: SorobanAddressCredentials,
    val delegates: List<SorobanDelegateSignature>,
) : XdrElement {
    override fun encode(stream: XdrOutputStream) {
        addressCredentials.encode(stream)
        stream.writeInt(delegates.size)
        delegates.forEach { it.encode(stream) }
    }

    companion object : XdrElementDecoder<SorobanAddressCredentialsWithDelegates> {
        override fun decode(stream: XdrInputStream): SorobanAddressCredentialsWithDelegates {
            val addressCredentials = SorobanAddressCredentials.decode(stream)
            val delegatesSize = stream.readInt()
            val delegates = (0..<delegatesSize).map {
                SorobanDelegateSignature.decode(stream)
            }
            return SorobanAddressCredentialsWithDelegates(
                addressCredentials,
                delegates,
            )
        }
    }
}
